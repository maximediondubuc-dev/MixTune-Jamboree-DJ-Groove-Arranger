import { AuthConfig } from "angular-oauth2-oidc";

export const environment = {
    spotifyAuthConfig : {
        issuer : '',
        redirectUri : '',
        clientId : '',
        responseType : 'code',
        scope : '',
        showDebugInformation:false
    }


};
